namespace sudoku
{

namespace constants
{

const char maxValue = '9';
const size_t boxSize = 3;
const size_t numBoxes = 9;
const size_t numRows = 9;
const size_t numColumns = 9;
const size_t numElements = numRows * numColumns;

}  // namespace constants

}  // namespace sudoku
